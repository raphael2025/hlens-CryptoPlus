{
  "source": "documented",
  "note": "docs/04-DATA-SOURCES.md §3: POST /info {\"type\":\"meta\"} (W=20) is the cheapest useful read — there is no ping. Trimmed to two universe entries, which is all a reachability probe looks at (it reads the status code, never the body). Hand-built, documented; M1-G records the real one from the production host's egress.",
  "status_code": 200,
  "payload": {
    "universe": [
      {"name": "BTC", "szDecimals": 5, "maxLeverage": 50},
      {"name": "ETH", "szDecimals": 4, "maxLeverage": 50}
    ]
  }
}
